apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    app.kubernetes.io/name: pilot
  name: {{ include "zesty-k8s.insights.monitoring.fullname" . }}-rolebinding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: metrics-exporter-role
subjects:
- kind: ServiceAccount
  name: {{ include "zesty-k8s.insights.monitoring.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
