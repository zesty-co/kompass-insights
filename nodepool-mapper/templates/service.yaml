apiVersion: v1
kind: Service
metadata:
  name: {{ include "nodepool-mapper.fullname" . }}
  labels:
    {{- include "nodepool-mapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
      name: grpc
  selector:
    {{- include "nodepool-mapper.selectorLabels" . | nindent 4 }}
